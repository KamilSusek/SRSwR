version: '2'
services:
  swsr-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/swsr/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=swsr
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
