import React from 'react';
import { Container, Button } from 'reactstrap';

const ReservationFilters = props => {
  return (
    <Container className="card" fluid>
      <Button>Filtruj</Button>
    </Container>
  );
};

export default ReservationFilters;